# Base URL for the API. Leave empty to use the Vite dev proxy (recommended in dev).
# For a deployed backend, set the full origin, e.g. https://api.example.com
VITE_API_BASE_URL=

# Where the Vite dev proxy forwards /api requests (dev only).
VITE_API_PROXY_TARGET=http://localhost:5000
